---
doc_id: project-optimization-backlog
title: 项目优化建议清单
version: 1.1.2
status: in_progress
last_updated: 2026-09-10
owner: Sean Lu
related:
  - path: docs/安全修复清单.md
    role: 安全专项清单（#13/#14/#18/#19/#20 与本清单存在交叉）
  - path: docs/三屏响应式优化方案.md
    role: 响应式专项主文档（v1.1.20），前端性能项 N-4/N-5/N-28 与其联动
---

# 项目优化建议清单

> **协作约定（供 IDE / AI 工具读取）**
> 1. 修改本文件须递增 `version` 并在「更新日志」**表格顶部**追加一行
> 2. 每完成一项，更新该项标题的状态标记与顶部「完成进度总览」
> 3. 与安全清单交叉的条目（#13/#14/#18/#19/#20）**以 `docs/安全修复清单.md` 为准**
> 4. 新增建议继续用 `N-xx` 编号，避免与原始 `#xx` 混淆

## 更新日志

| 版本 | 日期 | 修改方 | 变更摘要 |
|---|---|---|---|
| 1.1.2 | 2026-09-10 | WorkBuddy | **N-29 关闭**（8 处 `max-width:768px` → `767px`，新增白名单守卫 `test_breakpoints_use_canonical_values` 取代按值补黑名单的做法）；N-25 测试数 72 → **73**；同步 §16 计划中立即项 ① 完成 |
| 1.1.1 | 2026-09-10 | WorkBuddy | 逐条对照 2026-09-10 代码复核。**关闭 5 项**：#7（`pages/storage.py` 已删）、#20（`solarone-website-preview/` 已删）、N-26（根目录 29 个 `_*` 已清，仅剩 1 个）、N-27（`pages/views_old.py` 已删）；N-28 转🟨部分完成（字体异步已修、hero srcset 已落地，仅非首帧 lazy 未解）。**更新**：N-25 测试数 28 → **72**。**新增 2 项**：N-29（`max-width:768px` 与平板档边界重叠，8 处）、N-30（`.workbuddy/tmp/` 30+ 调试残留）。 |
| 1.1.0 | 2026-09-08 | WorkBuddy | 逐条对照当前代码复核完成进度（2026-09-08 实况）；新增文档头与进度总览；新增建议 N-22 ~ N-28；关闭 #8（已确认在用） |
| 1.0.0 | 2026-08-27 | — | 初稿：#1 ~ #21 优化建议与优先级 |

## 完成进度总览（2026-09-10 实况核对）

| 状态 | 数量 | 编号 |
|---|---|---|
| ✅ 已完成 | 14 | #1 #2 #4 #5 #7 #8 #13 #14 #15 #16 #17 #20 N-26 N-27 |
| 🟨 部分完成 | 4 | #3 #11 #12 N-28 |
| ❌ 未开始 | 6 | #6 #9 #10 #18 #19 #21 |
| 🆕 新增 | 2 | N-29 N-30 |

**较 v1.1.0 的变化**：#7 / #20 / N-26 / N-27 由「未开始」→「已完成」（2026-09-09/10 清理中落地）；N-28 由「未开始」→「部分完成」；N-25（无 CI）仍未开始，但测试数已从 28 增至 **73**。

**已确认关闭（原判断有误）**：#8 —— `ProductsPageCard` 实际在用（`admin.py:1032` 注册、`views_products.py:10` 解析图片、admin 模板引用），无需删除。

---

## 架构优化

### #2 ✅ views.py 单体巨石 → **已完成**

**原状**：`pages/views.py` 1197 行单体文件。
**现状**：已拆为 `pages/views/` 包，10 个模块共 1796 行：

| 模块 | 行数 | 职责 |
|---|---|---|
| `utils.py` | 519 | 图片/图库解析 |
| `i18n.py` | 260 | 语言映射字典 |
| `enrich.py` | 240 | 数据富化 |
| `views_products.py` | 232 | 产品视图 |
| `data_loaders.py` | 166 | seed 数据加载 |
| `views_contact.py` | 112 | 联系表单 |
| `views_other.py` | 102 | 其他页面 |
| `views_projects.py` | 87 | 项目视图 |
| `common.py` | 58 | 公共工具 |
| `__init__.py` | 20 | 导出 |

**遗留**：`pages/views_old.py`（82KB）仍是死代码，见 #20。

---

### #3 🟨 _SIDEBAR_I18N 硬编码翻译 → **部分完成**

**现状**：已提取到独立模块 `pages/views/i18n.py`（260 行），但**仍是硬编码字典，未迁移到 Django `.po` 标准翻译**。

`i18n.py` 中仍存在：
```python
_PRODUCT_CARD_LABELS = { 'm-series': 'Area and Site', ... }
_PRODUCT_CAT_TO_SIDEBAR_LABEL = { 'AREA_SITE': 'Area and Site', ... }
```

**建议**：
- 短期（低风险）：保持现状，已达成"改一处即可"的初步目标。
- 长期：迁移到 `.po`，用 `{% trans %}` / `gettext()`。**注意**：迁移时需处理 `poedit` 流程与 Vercel 构建时编译 `.mo` 的问题（当前 build 流程无 `compilemessages` 步骤，见 N-25）。

---

### #6 ❌ _COMPARE_IMAGE_SLUGS 硬编码 → **未开始**

**位置**：`pages/views/enrich.py:174`
```python
_COMPARE_IMAGE_SLUGS = frozenset({...})
...
project.has_compare_images = slug in _COMPARE_IMAGE_SLUGS   # L187
```
（`pages/views_old.py:822` 也有一份旧副本。）

**建议**：在 `Project` 模型增加 `compare_old_image` / `compare_new_image` 两个 `ImageField`，从后台配置；保留 slug 集合作为兜底直至数据迁移完成。

---

## 代码质量

### #7 ✅ OverwriteStorage 定义但从未使用 → **已完成（文件已删除）**

**原状**：`pages/storage.py:11` 定义 `class OverwriteStorage(FileSystemStorage)`，全项目**无任何引用**。

**现状（2026-09-09 核实）**：`pages/storage.py` **已删除**（备份在 `.workbuddy/archive/`）。采纳了原建议的「删除该文件」方案。

> 未走「启用它」路线——启用会改变 `upload_to` 行为（覆盖同名文件而非加哈希后缀），
> 属于功能性变更，需在明确需要「消除哈希后缀堆积」时再评估。当前 `media/` 堆积不构成实际问题。

---

### #8 ✅ ProductsPageCard 模型"可能未使用" → **已确认在用，关闭**

**核查结论**：该模型确实在使用，原文"需确认"的疑虑已排除：
- `pages/admin.py:19` 导入、`:1032` `@admin.register(ProductsPageCard)`
- `pages/views/views_products.py:10` / `:47` 解析其图片 URL
- `templates/admin/pages/products_page.html:217` 有对应管理界面

**无需删除。**

---

### #9 ❌ django_cleanup 被注释掉 → **未开始**

**位置**：`solarone/settings.py:113`
```python
# 'django_cleanup',  # Uncomment after installing: pip install django-cleanup
```

**建议**：二选一 —— ① 安装并启用（自动清理 ImageField 旧文件）；② 删除该注释避免误导。**推荐先删注释**：启用会引入新依赖，而 `media/` 堆积目前不构成实际问题。

---

### #10 ❌ 函数内部 `import os` → **未开始**

**位置**：`pages/views/views_products.py:20`、`:49`（两处函数内 `import os`）。文件顶部已有 `import os`。

**建议**：删除这两行。属 1 分钟改动，零风险。

---

### #11 🟨 缺少类型注解 → **部分完成**

**现状**：已有零星注解，如 `pages/views/utils.py:139` `def _clean_hashed_name(name: str) -> str:`，但同文件 `:310` `:335` `:400` 等大量函数仍无返回类型。

**建议**：优先给 `utils.py` / `enrich.py` 的公共函数补注解；不必追求全量覆盖。

---

### #12 🟨 缓存策略不一致 → **部分完成**

**现状**：
- ✅ 联系表单限流已改用 Django cache API 且 **fail-closed**（`pages/views/views_contact.py:4`、`common.py:2`）
- ❌ 产品/项目缓存仍是进程级 dict（`pages/views/enrich.py:16` `_enriched_products_cache`）

**影响判断**：Vercel 无状态下进程级 dict 缓存会在冷启动后失效重建，**不会出错，只是缓存命中率为 0**；而 seed 数据加载本身不慢，因此**实际影响有限，可降优先级**。

**建议**：维持现状；若后续 seed 数据增长导致首请求变慢，再引入外部缓存（Upstash Redis）。

---

## 安全 & 运维

### #13 ✅ SECRET_KEY 环境变量 → **已完成**
`solarone/settings.py:28-42`：无 `django-insecure-*` 回退；Vercel 缺失时抛 `ImproperlyConfigured`；本地自动生成随机 key 并打 warning。
> 需在 Vercel 控制台配置 `SECRET_KEY`，详见 `docs/安全修复清单.md` #2b。

### #14 ✅ 缺少 .env.example → **已完成**
根目录 `.env.example` 已存在。

### #17 ✅ index.py 调试日志 → **已完成**
入口已迁移至 `api/index.py`，`print(` 计数为 **0**。

### #18 ❌ Visitor 追踪每次 GET 写 DB → **未开始，但生产环境已规避**

**位置**：`pages/middleware.py:60` `Visitor.objects.create(...)`

**重要补充（2026-09-08 核查）**：
```python
if not IS_VERCEL:
    MIDDLEWARE.insert(1, 'whitenoise.middleware.WhiteNoiseMiddleware')
    MIDDLEWARE.append('pages.middleware.VisitorTrackingMiddleware')   # 仅本地挂载
```
→ **Vercel 生产环境未挂载该中间件**，因此"每次请求写 DB"的性能问题在生产**不存在**。

**但衍生出一个功能问题**：生产环境访客统计 `Visitor` / `DailyStats` **完全不生效**，后台看到的数据恒为空。

**建议**：明确取舍 ——
- 若不需要生产访客统计：现状即可，把本地也去掉或保留均可，文档注明。
- 若需要：改用外部分析（Plausible / GA），或改为按 IP+日 去重的低频写入。

---

## 可清理文件

### #19 ❌ scripts/verify_static_hashes.py → **未开始（2026-09-10 仍存在）**

仍在 `scripts/`，硬编码 `localhost:8000` 的一次性脚本。

**2026-09-10 复核**：文件仍存在。同目录现有 `dev_preview.py`（常驻预览）与 `e2e/`（回归脚本），
`verify_static_hashes.py` 与二者职能重叠且已过时。**建议删除**，或移入 `scripts/_archive/`。

### #20 ✅ solarone-website-preview/ 目录 → **已完成（已删除）**

**现状（2026-09-09 核实）**：目录**已不存在**——在 v1.4.0 的整体清理中随 `screenshots/`（16MB）、`staticfiles/`（66MB）一并移除，共回收约 87MB。

### #21 ❌ public/.gitkeep → **未开始（2026-09-10 仍存在）**

`public/.gitkeep` 仍存在。**风险极低**（`build.sh` 每次重建 `public/`，且 `.gitignore` 已忽略，未入库），属无意义文件。

**建议**：删掉即可；或保留——若 `build.sh` 依赖 `public/` 目录存在，删除后需确认构建仍正常。

---

## 新增建议（v1.1.0，2026-09-08 核查后）

> 编号 `N-xx`，与原始 `#xx` 区分。

### 🟠 N-22 `pages/seed_data.py` 3183 行 —— 已成为新的最大文件

**现状**：比当初被诟病的 `views.py`(1197 行) 还大 **2.7 倍**。当前行数 Top 5：

| 文件 | 行数 |
|---|---|
| `pages/seed_data.py` | **3183** |
| `pages/admin.py` | 1246 |
| `pages/models.py` | 925 |
| `pages/seed_sync.py` | 620 |
| `pages/views/utils.py` | 519 |

**建议**：仓库已有 `seed_data.json`（1.5MB）。优先方案是**让 `seed_data.py` 退化为 JSON 的薄加载器**，把硬编码数据外置到 JSON；其次才是按数据域拆分（products / projects / news / siteconfig）。
> 收益：数据变更不再需要改代码，且消除"改数据触发全量 Python 重载"的部署风险。

### 🟠 N-23 `pages/admin.py` 1246 行 —— 建议拆包

已注册数十个 ModelAdmin 并含自定义 action。建议拆为 `pages/admin/` 包（`__init__.py` 汇总 + 按领域分文件），风险低、收益明确。

### 🟡 N-24 `pages/views/utils.py` 519 行 —— 可再拆

拆分后仍是最大的视图模块，混装了图片清理、图库扫描、seed 路径解析三类职责。建议拆为 `utils_image.py` / `utils_gallery.py` / `utils_seed.py`。

### 🟠 N-25 无 CI —— **73 个测试仍未自动执行**

仓库**无 GitHub Actions**（2026-09-10 核实仍无 `.github/` 目录）。`pages/tests.py` 现有 **72 个用例**（v1.1.0 时为 28，两轮响应式改造新增 44 条防回归），仅能手动跑。

**建议**：加一个最小 workflow，push/PR 时执行：
```yaml
- run: pip install -r requirements.txt
- run: python manage.py test
```
后续可把 `docs/三屏响应式优化方案.md` §6 的 L1/L2 挂进去做回归基线。
> 注意：若后续要迁移 `.po`（#3），需在 build 流程补 `compilemessages`。

> **优先级上升说明**：测试数从 28 → 72，手动跑的成本与漏跑风险同步上升；
> 且本轮已出现过「改了 CSS/模板但没跑测试、问题只在真机暴露」的情况。建议尽快补 CI。

### ✅ N-26 根目录 `_*` 临时文件 → **已完成（29 个 → 1 个）**

**原状**：根目录 29 个 `_*` 文件（`_*.py` / `_*.txt` / `_*.ps1`），混有敏感文件 `_secret_key.txt`。

**现状（2026-09-10 核实）**：
- 根目录 `_*` 仅剩 **1 个**（`_l2_server.log`，L2 脚本的服务器日志）
- `_secret_key.txt` **已删除**（生产 SECRET_KEY 走 Vercel 环境变量，实测站点 200 正常）→ 安全清单 N-23 同步关闭

**但衍生新问题**：调试残留转移到了 `.workbuddy/tmp/`（30+ 个 `*.txt`），见 **N-30**。

### ✅ N-27 `pages/views_old.py` 82KB 死代码 → **已完成（已删除）**

**现状（2026-09-09 核实）**：`pages/views_old.py` **已删除**（备份在 `.workbuddy/archive/`），
`_COMPARE_IMAGE_SLUGS` 等旧副本不再干扰 grep 排查。2026-09-10 复核确认未回归。

> ⚠️ 当时的操作教训（永久铭记）：删除时**不要**用 `git rm` —— 曾因 `.git/index.lock` 残留
> 导致整个 `pages/` 目录被误删。改用 `rm -f <file>` + `git add -A`，删完立刻 `test -d <父目录>` 验证。

### 🟨 N-28 前端性能：图片无 `srcset`、字体阻塞渲染 → **部分完成**

| 子项 | 原状 | 现状（2026-09-10） |
|---|---|---|
| 字体阻塞渲染 | `base.html` Google Fonts 阻塞式 `<link>` | ✅ **已修**（N-5）：`media="print" onload="this.media='all'"` 异步加载 + `<noscript>` 兜底 |
| hero 无 srcset | 1920px 图直出手机 | ✅ **已修**（N-4）：移动竖版 `<picture>`(≤767px) + 桌面 `srcset 1280w/1920w` + `sizes` 对齐 F7 断点 |
| 非首帧 lazy 失效 | `loading="lazy"` 因绝对定位铺满视口不生效 | ❌ **未解**：三张 slide 都是 `position:absolute; inset:0`，浏览器一律判定为「在视口内」，lazy 不触发。已记录，待后续改为「首帧 eager + 其余 JS 按需插入 `src`」 |

详见 `docs/三屏响应式优化方案.md` §2.5 的 **N-4 / N-5**（含修复方案）与 §13（hero 竖版规格）。

### ~~🟠 N-29 F7 断点统一有漏网：8 处 `max-width: 768px` 与平板档边界重叠~~ ✅ **已关闭（2026-09-10，v1.1.2）**

> **已修复并加防回归。** 8 处全部改 `767px`；防回归**没有沿用黑名单写法**，
> 而是新增白名单守卫 `test_breakpoints_use_canonical_values`——任何 `@media` 宽度必须落在
> `max-width ∈ {767,1024,1199}` / `min-width ∈ {768,1200}`，一次性封死未来任何新漏网值。
> L1 **73 tests OK（skipped=2）** + L2 **ALL CHECKS PASSED**（含 768×1024 iPad 竖屏视口）。
> 详见 `docs/三屏响应式优化方案.md` §2.5 **N-31**。以下为原始记录，保留备查。

**发现时间**：2026-09-10（v1.4.2 阶段二复核）

**背景**：F7 把全站断点收敛为三档 `≤767 / 768–1199 / ≥1200`，并加了测试断言
`test_no_legacy_900px_breakpoint_in_source`（正则 `@media\s*\(\s*max-width:\s*900px\s*\)`）守住 900px。
**该断言已通过**，F7 的 900px → 767px 改造确实彻底。

**但漏了一类**：源码中仍残留 **8 处 `@media (max-width: 768px)`**（不含在 900px 断言的匹配范围内）：

| 文件 | 行号 |
|---|---|
| `static/css/base.css` | 316, 1979, 2000, 2055, 2091, 2100 |
| `templates/about.html` | 284 |
| `templates/contact.html` | 171 |

**问题**：`max-width:768px` 与平板档起点 `min-width:768px`（base.css 中有 20+ 处）在 **768px 这个点上同时命中**。
CSS 媒体查询在同优先级下**后定义者胜出**，因此 768px 处的实际表现取决于源码书写顺序——
这正是「断点统一」要消除的那一类不确定性。iPad 竖屏（768pt）恰好落在这个点上。

**建议**：
1. 把这 8 处统一改为 `max-width: 767px`（与 F7 的手机档定义一致）
2. 补一条测试断言：源码中不得出现 `@media (max-width: 768px)`，只能出现 `767px`
   （与现有 900px 断言同构，正则 `@media\s*\(\s*max-width:\s*768px\s*\)`）
3. 用 `scripts/e2e/run_checks.py` 在 **768px 视口**跑一次，确认改后无回归

> 关联：`docs/三屏响应式优化方案.md` 记为 N-31（响应式主文档的编号），两处指向同一问题。

### 🟡 N-30 `.workbuddy/tmp/` 堆积 30+ 个调试输出

**发现时间**：2026-09-10

**现状**：v1.4.2 阶段二跑 L1/L2 时把大量中间输出重定向到了 `.workbuddy/tmp/`：

```
l1_phase2b.txt (76K)  l1_tail2b.txt  l1_phase2c.txt  l1_phase2d.txt  l1_fail2b.txt
l2_phase2.txt (16K)   l2_check.txt   l2_check2.txt   l2_phase2b.txt  l2_phase2b_err.txt
cs.txt  cs_exit.txt   gs2.txt  gs3.txt  gitstatus.txt  herofiles.txt
netstat.txt  procs.txt  rc_diff.txt  shots.txt  gen_hero_out.txt  gen_hero_exit.txt
```

**评估**：`.workbuddy/` 已被 `.gitignore` 忽略，**不影响仓库**，性质与 N-26 的「根目录残留」不同，风险低。
但会拖慢目录浏览、干扰「哪些才是永久工具」的判断。

**建议**：
- **保留 4 个永久探针模板**：`probe_font.py`（CSS 变量实测）、`probe_align.py`（元素对齐实测）、
  `probe_button_width.py`（按钮尺寸实测）、`probe_panel_buttons.py`（抽屉按钮位置实测）——
  这四个是排查同类回归的标准工具，已在 §14 文档中作为模板引用
- **保留 `gen_hero_1280.py` 但搬到 `scripts/`**：它是生成 1280 档 hero 的脚本，属可复用资产，
  留在 tmp 里下次找不到；搬走后改名 `scripts/gen_hero_variants.py` 并在文档中登记
- **删除其余 `*.txt`** 调试输出

---

## 优先级建议（2026-09-10 更新）

| 优先级 | 编号 | 影响 | 说明 |
|---|---|---|---|
| 🔴 高 | **N-25** | 质量保障 | 73 个测试仍靠手动跑，无 CI |
| 🟠 中 | **N-22** | 可维护性 | 3183 行新巨石，比原 views.py 更严重 |
| 🟠 中 | **N-23** | 可维护性 | admin.py 1246 行，建议拆包 |
| 🟠 中 | **#6** | 可维护性 | 对比图硬编码，新增项目需改代码 |
| 🟠 中 | **#18** | 功能 | 生产访客统计实际不生效（需明确取舍） |
| 🟡 低 | **#9 #10 #19 #21** | 代码整洁 | 均为分钟级改动 |
| 🟡 低 | **N-24 / N-28 / N-30** | 可维护性 / 性能 / 清洁 | 可排入后续迭代 |
| ⏸ 观察 | **#3 #11 #12** | — | 已部分完成，实际影响有限 |
| ✅ 关闭 | **#7 #8 #20 N-26 N-27 N-29** | — | 本轮复核确认已完成（N-29 于 v1.1.2 关闭）|
