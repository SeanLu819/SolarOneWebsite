# 项目安全修复清单

> 生成日期：2026-09-02
> 本清单记录全部待处理安全问题的优先级、位置与修复方案。
> 每项修复遵循「先修改 → 测试通过 → 提交」流程。

---

## ✅ 已完成（已提交）

| 编号 | 问题 | 提交 | 说明 |
|------|------|------|------|
| #1 | `admin_translate` 端点无认证 + `@csrf_exempt` | `032babd` | 已加 `@staff_member_required`，移除 `csrf_exempt`，净化外部 API 错误消息 |
| #2 | `SECRET_KEY` 硬编码不安全回退 | `4fac0f5` | 删除 `django-insecure-*` 回退；Vercel 缺失时抛 `ImproperlyConfigured`；本地自动生成随机 key |
| #3 | `DEBUG` 默认开启 | `54b4e08` | 默认改为 `False`，需显式 `DEBUG=true` 才开启 |
| #18 | 缺少 `.env.example` | `4fac0f5` | 已创建，含必需/可选环境变量说明 |
| #4 | 公开诊断端点 `/__diag__/` | `fb0fcb5` | 仅 `DEBUG=True` 且 `staff_member_required` 可访问，生产环境返回 404 |
| #5 #22 | 缺失安全响应头 + 代理头 | `99e8d11` | Vercel 强制 HTTPS + HSTS(1年/preload) + `SECURE_PROXY_SSL_HEADER`；全环境 `nosniff` |
| #6 | 缺失 Cookie 安全设置 | `99e8d11` | Vercel 上 `SESSION/CSRF_COOKIE_SECURE=True`；全环境 `SameSite=Lax`；`CSRF_COOKIE_HTTPONLY` 保持 False（auto_translate.js 需读取） |
| #7 | `WHITENOISE_ALLOW_ALL_ORIGINS=True` | `99e8d11` | 已改为 `False`，静态资源不再返回 `Access-Control-Allow-Origin:*` |

---

## ⏳ 待部署操作（需您在 Vercel 控制台完成）

### #2b — 在 Vercel 配置 `SECRET_KEY` 环境变量

> ⚠️ **必须完成**，否则生产部署会因 `ImproperlyConfigured` 拒绝启动（有意设计）。

- 强随机密钥已生成，保存在本地 `_secret_key.txt`（已被 .gitignore 忽略，**请打开该文件复制密钥**）：
- **操作路径**：Vercel 控制台 → Project → **Settings → Environment Variables**
  - Name: `SECRET_KEY`
  - Value: 上面密钥（注意 `*`、`#`、`+` 等字符不要被编辑器转义）
  - Apply to: **Production**（建议同时加到 Preview / Development）
- **CLI 方式**（需先安装 Node.js + `npm i -g vercel` 并 `vercel login`）：
  ```bash
  vercel env add SECRET_KEY production
  vercel env add SECRET_KEY preview
  vercel env add SECRET_KEY development
  ```
- **REST API 方式**（需 `VERCEL_TOKEN`）：
  ```bash
  curl -X POST https://api.vercel.com/v9/projects/<PROJECT_ID>/env \
    -H "Authorization: Bearer $VERCEL_TOKEN" \
    -H "Content-Type: application/json" \
    -d '{"key":"SECRET_KEY","value":"<见 _secret_key.txt>","type":"encrypted","target":["production"]}'
  ```
- **验证**：部署后访问任意页面，确认 500 错误消失；或 `vercel env ls` 看到记录。

---

## 🟠 高优先级（已全部完成 ✅）

#4、#5、#6、#7、#22 均已修复并通过测试（见上表）。

---

## 🟡 中优先级（✅ 8/9 已完成）

### #8 — 模板 `|safe` 过滤器的存储型 XSS ✅ 已修复（`3e16c2f`）
- **修复**：移除全部 9 处 `|safe`（home×4、about×4、projects×1），恢复自动转义

### #9 — `nl2para` 过滤器 XSS ✅ 已修复（`3e16c2f`）
- **修复**：`conditional_escape()` 后再包裹 `<p>`/`<br>`；管理员内容中的 HTML 一律字面渲染；已确认现有 seed 数据无内联 HTML，不影响显示

### #10 — `linebreaktospaces` 过滤器不转义 ✅ 已修复（`3e16c2f`）
- **修复**：非 SafeData 输入先 `escape()`；SafeData（如来自 `linebreaks`）保持原有去 `<br>` 行为

### #11 — CSS 注入（admin 控制的 CSS 自定义属性）✅ 已修复（`4d57625`）
- **修复**：`SiteConfig` 的颜色/字号/字体字段加正则 Validator，`full_clean` 强制校验

### #12 — `deep_translator` 未列入依赖 ✅ 已修复（`3e16c2f`）
- **修复**：`requirements.txt` 增加 `deep-translator>=1.11`

### #13 — 联系表单速率限制「失败开放」✅ 已修复（`3e16c2f`）
- **修复**：缓存不可用时拒绝请求（fail-closed）并有测试覆盖

### #14 — 速率限制为每实例（Vercel 无分布式缓存）⏸ 待决策
- **需引入外部服务**（Upstash Redis / Vercel Edge 限流），按当前低流量联系表单可暂接受 best-effort； honeypot（#15）已兜底

### #15 — 联系表单无防垃圾机制 ✅ 已修复（`3e16c2f`）
- **修复**：增加 honeypot 隐藏字段（`company_website`），机器人提交被静默丢弃（假成功），真人正常提交不受影响；有测试覆盖

### #16 — Visitor 追踪存原始 IP ✅ 已修复（`b00a2d1`）
- **修复**：IP 以 `SECRET_KEY` 加盐 SHA-256 哈希存储（`GenericIPAddressField` → `CharField(64)`，迁移 `0023`）；唯一访问计数逻辑不变；有测试覆盖

### #17 — Host 头注入 / canonical URL 投毒 ✅ 已修复（`d3e97bc`）
- **修复**：新增 `CANONICAL_ORIGIN` 设置 + context processor；canonical/og:url/JSON-LD/hreflang/sitemap.xml/robots.txt 全部改用固定域名，不再取自 `request.get_host()`；附带修复 `sitemap.xml` 模板缺失导致端点 500 的老 bug

---

## 🟢 低优先级（清理）

### #19 — 仓库根目录遗留调试/临时脚本
- **文件**：`_*.py` `_*.txt` `_*.ps1` 等约 28 个
- **方案**：删除或归档到 `scripts/_archive/`

### #20 — `pages/views_old.py` 死代码（82KB）
- **位置**：`pages/views_old.py`
- **方案**：删除或移入归档目录

### #21 — `db.sqlite3` 位于工作目录
- **位置**：根目录
- **现状**：已被 `.gitignore` 忽略未入库；确认开发库不含敏感数据即可
- **方案**：保持忽略；必要时用独立本地库

---

## 处理顺序（按优先级）

1. 🟠 **#4** 诊断端点（先移除/锁死）
2. 🟠 **#5 + #6 + #22** 安全头 + Cookie（settings.py 一次性补齐，本地可测试）
3. 🟠 **#7** WhiteNoise CORS 关闭
4. 🟡 **#9 + #10** 模板过滤器转义（XSS）
5. 🟡 **#8** `|safe` 清理
6. 🟡 **#11** CSS 注入字段校验
7. 🟡 **#12** 依赖补齐 + 翻译端点验证
8. 🟡 **#13** 速率限制 fail-closed
9. 🟡 **#17** ALLOWED_HOSTS 收紧
10. 🟢 **#19/#20** 清理
11. 🟡 **#14/#15/#16**（需用户决策：是否引入外部服务）